# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ZCRM-iOS-CustomerPortal-SampleApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

pod 'ZCRMiOS', :git => 'https://github.com/zoho/CRM-iOSSDK.git', :branch => 'V2_ALPHA'

  # Pods for ZCRM-iOS-CustomerPortal-SampleApp

  target 'ZCRM-iOS-CustomerPortal-SampleAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
